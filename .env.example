# OpenAI API Key for contract generation (main process)
# Copy this file to .env.local and add your real API key
# Note: electron-vite automatically loads this file
OPENAI_API_KEY=your-openai-api-key-here

# GitHub token for auto-updates (optional)
GH_TOKEN=your-github-token-here

# Debug mode for renderer process (use VITE_ prefix for renderer variables)
VITE_DEBUG_MODE=true
